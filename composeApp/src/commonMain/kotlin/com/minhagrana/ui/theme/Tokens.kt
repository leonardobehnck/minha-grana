package com.minhagrana.ui.theme

import androidx.compose.ui.unit.dp

object Borders {
    val input = 10.dp // text fields
    val card = 14.dp // all floating card containers
    val selector = 18.dp // chips, selectors, pill-like elements
    val dialog = 18.dp // dialogs and full-screen form containers
}

object Elevation {
    val none = 0.dp
    val card = 4.dp // all floating card containers
    val raised = 8.dp // cards with stronger emphasis (buttons)
    val modal = 16.dp // dialogs, bottom sheets
}
